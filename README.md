# A gfxprim retro arch frontend

A simple retro arch frontend for a gfxprim.

To run a DOS game you need to install dosbox libretro core and run it as:

```
gparch /usr/lib64/libretro/dosbox_libretro.so dosgame/dosgame.exe
```
